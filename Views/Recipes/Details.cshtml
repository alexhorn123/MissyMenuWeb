@model MissyMenu.Service.Recipe

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Recipe</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    @for (var i = 0; i < Model.Ingredients.Count; i++)
        {
            <dl class="row">
                <dt class = "col-sm-2">
            @Html.DisplayNameFor(item => item.Ingredients.ElementAt(i).IngredientName)
        </dt>
                <dt class = "col-sm-10">
            @Html.DisplayFor(item => item.Ingredients.ElementAt(i).IngredientName)
        </dt>
    </dl>
    }

        @for (var i = 0; i < Model.Directions.Count; i++)
        {
            <dl class="row">
                <dt class = "col-sm-2">
            @Html.DisplayNameFor(item => item.Directions.ElementAt(i).Step)
        </dt>
                <dt class = "col-sm-10">
            @Html.DisplayFor(item => item.Directions.ElementAt(i).Step)
        </dt>
    </dl>
    }

</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
